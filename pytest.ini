[pytest]
# Suppress specific RuntimeWarnings from test mocks
# These are test artifacts where mock coroutines aren't being properly awaited
# The actual code correctly uses run_async() to execute them
filterwarnings =
    ignore::RuntimeWarning
