# Runtime dependencies (includes shared base + flexible torch entries)
-r requirements-base.txt

# PyTorch (CPU by default) - allow GPU pack override at runtime.
torch>=2.0.0,<2.5.0
torchaudio>=2.0.0,<2.5.0
