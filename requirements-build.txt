# Build-time dependencies for CI/CD pipelines
# This file pins CPU-only PyTorch to ensure small executable sizes (~350-500 MB)
#
# For development, use requirements.txt instead (allows GPU PyTorch)
# For GPU development: run.bat install --gpu

# ===== Runtime dependencies (same as requirements.txt) =====
aiofiles==24.1.0
pillow==10.4.0
PySide6==6.6.3.1
PySide6_Addons==6.6.3.1
PySide6_Essentials==6.6.3.1
shiboken6==6.6.3.1
librosa==0.10.1
demucs==4.0.1
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
soundfile>=0.12.1
watchdog>=4.0.0
matplotlib>=3.5.0

# ===== PyTorch - CPU-only for builds =====
# Using CPU-only versions to minimize executable size
# GPU Pack system provides optional CUDA acceleration at runtime
# CRITICAL: Use --index-url (not --extra-index-url) to force CPU-only wheels
# This prevents pip from falling back to PyPI's CUDA-bundled wheels on Linux
--index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
torchaudio==2.4.1+cpu
